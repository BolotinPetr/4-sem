class Nvector:
    def __init__(self, coords):
        self.n = len(coords)
        self.coords = coords
    def set_coords(self, new_coords):
        self.coords = new_coords
    def print_coords(self):
        print self.coords
    def __add__(self, vector):
        newVector = self
        if (vector.n == newVector.n):
            for i in range(len(newVector.coords)):
                newVector.coords[i] = newVector.coords[i] + vector.coords[i]
        return newVector
    def sub(self, vector):
        for i in range(len(vector.coords)):
            self.coords[i] -= vector.coords[i]
    def scalarProduct(self, vector):
        for i in range(len(vector.coords)):
            self.coords[i] *= vector.coords[i]

vector1 = Nvector([1, 4, 5])
vector2 = Nvector([1, 10, 13])
vector1.scalarProduct(vector2)
vector1.print_coords()
